<section class="connections-container">
	{{ai.serialNr}}
	<fieldset>
		<legend>Your properties</legend>
		<h1>connections</h1>
		<ul>
			{{#each this}}
					<li>
						<h2>{{instanceName}}</h2>
						<a class="btn--primary" class="global-nav__menu__myAi">Disconnect</a>
					</li>
			{{else}}
			<p>No connections available.</p>
			{{/each}}
		</ul>
	</fieldset>
</section>